# Steven's Week 9 Notes

## What I did

- Completed Task 9.4
- Completed Task 9.5
- Completed Task 9.6
- Integrated and updated `index.html` and `style.css`. *(Note: Mario's version of `app.js` will no longer work with this updated version due to the newly written functions in `app_Steven.js`.)*

### How to run the interface
To run the interface, use either of these methods:

**Method 1**
1. Open a terminal in the backend folder and run `python app.py`.
2. Simply double-click the `index.html` file in your folder

**Method 2**
1. Open a terminal in the backend folder and run `python app.py`.
2. Open a new terminal in the frontend folder and run `python -m http.server 8080`.
3. Navigate to `http://localhost:8080` in your web browser.